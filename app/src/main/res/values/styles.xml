<resources>

    <style name="Theme.Base" parent="AppTheme"> </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="selectableItemBackground">@drawable/ripple_selector</item>
        <item name="android:selectableItemBackground">@drawable/ripple_selector</item>
        <item name="colorControlHighlight">@color/item_pressed</item> 
    </style>

</resources>
